---
hideEditInGitHub: true
---

[**cc-everywhere**](../../../../../index.md)

<HorizontalLine />

# ~~Interface: TargetDocConfig~~

## Deprecated

Provide the workflow's own DocConfig (e.g. Module.EditImageDocConfig)
with an `asset` instead of a bare target `url`. Retained for backward
compatibility — still accepted at runtime — but no longer surfaced as an input
option in tooling.

## Properties

| Property | Type |
| ------ | ------ |
| ~~`url`~~ | `string` |
