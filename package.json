{
  "private": true,
  "name": "cc-everywhere",
  "version": "1.0.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/AdobeDocs/cc-everywhere"
  },
  "dependencies": {
    "@adobe/gatsby-theme-aio": "^4.1.1",
    "gatsby": "^4.12.1",
    "react":"^17.0.0",
    "react-dom":"^17.0.0"
  },
  "scripts": {
    "start": "gatsby build && gatsby serve",
    "start:prefix": "gatsby build --prefix-paths && gatsby serve --prefix-paths",
    "dev": "gatsby develop",
    "dev:https": "gatsby develop --https --host localhost.corp.adobe.com --port 9000",
    "build": "gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean"
  }
}
